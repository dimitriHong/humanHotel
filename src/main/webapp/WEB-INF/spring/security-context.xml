<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">
	<bean id="bcryptPasswordEncoder"
		class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" />
	<bean id="customLoginSuccess"
		class="javalec.hotel.hpro.security.CustomLoginSuccessHandler" />
	<security:http>

		<security:intercept-url pattern="/notice/**"
			access="permitAll" />
		<security:intercept-url pattern="/main/**"
			access="permitAll" />
		<security:intercept-url pattern="/restBoard/**"
			access="permitAll" />
		<security:intercept-url pattern="/member/mem_login" access="isAnonymous()" />
		<security:intercept-url pattern="/member/memberjoin" access="isAnonymous()" />
		<security:intercept-url
			pattern="/member/mypage/**" access="hasRole('ROLE_MEMBER')" />
		<security:form-login
			login-page="/member/mem_login"
			authentication-success-handler-ref="customLoginSuccess" />
		<!-- <security:logout logout-url="/member_mypage/mem_logout" invalidate-session="true" /> -->
		<security:logout
logout-url="/member_mypage/mem_logout"
logout-success-url="/"
invalidate-session="true"
delete-cookies="JSESSIONID"/>
<security:csrf disabled="true"/>
	</security:http>
	<security:authentication-manager>

		<security:authentication-provider>

			<security:jdbc-user-service
				data-source-ref="dataSource"
				users-by-username-query="select ID,PASSWORD,ENABLED from HmemberManage where ID = ? "
				authorities-by-username-query="select ID,AUTH from HmemberManage where ID = ?" />

			<security:password-encoder
				ref="bcryptPasswordEncoder" />
		</security:authentication-provider>

	</security:authentication-manager>
	
</beans>
